apiVersion: v1
kind: Service
metadata:
  name: webapp-service
  namespace: fp
spec:
  type: LoadBalancer
  selector:
    app: employees
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
